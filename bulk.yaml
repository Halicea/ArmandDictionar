# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Dictionary
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: AddedBy
      external_name: AddedBy
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Person')
      export_transform: transform.key_id_or_name_as_string

    - property: DateAdded
      external_name: DateAdded
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: Language1
      external_name: Language1
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Language')
      export_transform: transform.key_id_or_name_as_string

    - property: Language2
      external_name: Language2
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Language')
      export_transform: transform.key_id_or_name_as_string

    - property: Name
      external_name: Name
      # Type: String Stats: 1 properties of this type in this kind.

    - property: WordCount
      external_name: WordCount
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Language
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: AddedBy
      external_name: AddedBy
      # Type: Key Stats: 2 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Person')
      export_transform: transform.key_id_or_name_as_string

    - property: Code
      external_name: Code
      # Type: String Stats: 2 properties of this type in this kind.

    - property: DateAdded
      external_name: DateAdded
      # Type: Date/Time Stats: 2 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: Name
      external_name: Name
      # Type: String Stats: 2 properties of this type in this kind.

    - property: TotalWordCount
      external_name: TotalWordCount
      # Type: Integer Stats: 2 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)


- kind: Person
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: AuthenticationType
      external_name: AuthenticationType
      # Type: String Stats: 5 properties of this type in this kind.

    - property: DateAdded
      external_name: DateAdded
      # Type: Date/Time Stats: 5 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: Email
      external_name: Email
      # Type: Email Stats: 4 properties of this type in this kind.
      import_transform: db.Email

    - property: IsAdmin
      external_name: IsAdmin
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: Name
      external_name: Name
      # Type: String Stats: 5 properties of this type in this kind.

    - property: Notify
      external_name: Notify
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: Password
      external_name: Password
      # Type: String Stats: 5 properties of this type in this kind.

    - property: PhotoUrl
      external_name: PhotoUrl
      # Type: Link Stats: 2 properties of this type in this kind.
      import_transform: db.Link

    - property: Public
      external_name: Public
      # Type: Boolean Stats: 5 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: Surname
      external_name: Surname
      # Type: String Stats: 5 properties of this type in this kind.

    - property: UserName
      external_name: UserName
      # Type: String Stats: 5 properties of this type in this kind.

    - property: class
      external_name: class
      # Type: String Stats: 5 properties of this type in this kind.


- kind: Session
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: global_names
      external_name: global_names
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text

    - property: globals
      external_name: globals
      # Type: Blob Stats: 1 properties of this type in this kind.
      import_transform: transform.blobproperty_from_base64
      export_transform: base64.b64encode

    - property: unpicklable_names
      external_name: unpicklable_names
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text

    - property: unpicklables
      external_name: unpicklables
      # Type: Text Stats: 13 properties of this type in this kind.
      import_transform: db.Text


- kind: Word
  connector: csv # TODO: Choose a connector here: csv, simplexml, etc...
  connector_options:
    # TODO: Add connector options here--these are specific to each connector.
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: DateAdded
      external_name: DateAdded
      # Type: Date/Time Stats: 1 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: Dictionary
      external_name: Dictionary
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.create_foreign_key('Distionary')
      export_transform: transform.key_id_or_name_as_string

    - property: Translation
      external_name: Translation
      # Type: Text Stats: 1 properties of this type in this kind.
      import_transform: db.Text

    - property: Value
      external_name: Value
      # Type: String Stats: 1 properties of this type in this kind.

